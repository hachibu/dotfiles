#!/usr/bin/env crystal

require "colorize"

run_cmd = ->(cmd : String) do
  puts "#{"==>".colorize.blue} #{cmd}"
  system(cmd) || abort(cmd)
end

cmds = [
  "brew update --force",
  "brew prune",
  "brew cleanup --prune=1",
  "brew missing",
  "brew doctor"
]

cmds.each(&run_cmd)
