#!/usr/bin/env crystal

require "colorize"

cmds = [
  "update --force",
  "prune",
  "cleanup --prune=0",
  "missing",
  "doctor"
].map { |s| s.insert(0, "brew ") }

cmds.each do |cmd|
  puts "==> ".colorize.blue.to_s + cmd
  system(cmd)
end
