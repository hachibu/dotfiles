#!/usr/bin/env crystal

require "colorize"

brew = ->(cmd : String) do
  puts("#{"==>".colorize.blue} brew #{cmd}")
  system("brew #{cmd} > /dev/null")
end

%w(update prune cleanup missing doctor).each(&brew)
